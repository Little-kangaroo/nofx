# 道氏理论相关代码完整文档

## 概述

本文档详细记录了AI自动交易系统中道氏理论(Dow Theory)相关的逻辑代码，包括趋势识别、高低点判断、趋势强度评估等核心算法。道氏理论是技术分析的基石，为AI提供趋势方向和强度的关键判断依据。

---

## 一、核心文件列表

### 1.1 主要道氏理论文件

| 文件路径 | 行数 | 主要功能 |
|---------|------|---------|
| `market/dow_theory.go` | 1700+ | 道氏理论分析引擎，趋势识别和评估 |
| `market/types.go` | 900+ | 数据结构定义 |
| `market/data.go` | 3050+ | 辅助技术指标计算 |
| `market/utils.go` | 241 | 数学工具函数 |

### 1.2 集成文件

- `market/comprehensive_analysis.go` - 多时间框架综合分析
- `market/anchor_adapters.go` - 锚点转换适配器
- `market/context_scoring.go` - 上下文评分计算

---

## 二、数据结构定义

### 2.1 核心数据结构

#### DowTheoryData - 道氏分析主输出结构
```go
type DowTheoryData struct {
    SwingPoints   []*SwingPoint    // 摆动点集合
    TrendLines    []*TrendLine     // 趋势线集合
    Channel       *ParallelChannel // 平行通道
    TrendStrength *TrendStrength   // 趋势强度评估
    TradingSignal *TradingSignal   // 交易信号
}
```

**用途**: 包含完整的道氏理论分析结果，传递给AI进行决策

---

#### SwingPoint - 摆动点结构
```go
type SwingPoint struct {
    Type      SwingType // "high" 或 "low"
    Price     float64   // 价格
    Time      int64     // 时间戳（毫秒）
    Index     int       // K线索引
    Strength  float64   // 强度 (0-10)
    Confirmed bool      // 是否已确认
}
```

**强度评分机制**:
- 价格范围权重 × (1 - VolumeWeight)
- 成交量权重 × VolumeWeight
- 范围: 0-10

**用途**: 识别市场转折点，构建趋势线的基础

---

#### TrendLine - 趋势线结构
```go
type TrendLine struct {
    Type      TrendLineType // "support" 或 "resistance"
    Points    []*SwingPoint // 构成的摆动点
    Slope     float64       // 斜率（每单位时间的价格变化）
    Intercept float64       // 截距
    Strength  float64       // 强度评分 (0-10)
    Touches   int           // 触及次数
    LastTouch int64         // 最后触及时间
    Broken    bool          // 是否被突破
}
```

**趋势线公式**:
```
price = slope × time + intercept
```

**强度评分**:
```
强度 = touches × 1.0
     + min(timeSpan天数 / 10, 2.0)
     + (Σ point.Strength / points数) × 0.5
     + 角度奖励 (15° < angle < 75°: +0.5)
```

---

#### TrendStrength - 趋势强度结构
```go
type TrendStrength struct {
    Overall       float64        // 总体强度 (0-100)
    ShortTerm     float64        // 短期强度 (基于5m)
    LongTerm      float64        // 长期强度 (基于4h)
    Direction     TrendDirection // "up"/"down"/"flat"
    Quality       TrendQuality   // "strong"/"moderate"/"weak"
    Momentum      float64        // 动量强度
    Consistency   float64        // 一致性评分
    VolumeSupport float64        // 成交量支撑度
}
```

**评分机制**:
```
Overall = ShortTerm × 40% + LongTerm × 60% × VolumeSupport惩罚系数

VolumeSupport惩罚:
- 支撑度 < 40: 乘以 0.7
- 支撑度 ≥ 40: 无惩罚
```

---

#### DowTheoryConfig - 配置结构
```go
type DowTheoryConfig struct {
    SwingPointConfig SwingPointConfig // 摆动点配置
    TrendLineConfig  TrendLineConfig  // 趋势线配置
    ChannelConfig    ChannelConfig    // 通道配置
    SignalConfig     SignalConfig     // 信号配置
    ThresholdConfig  ThresholdConfig  // 动态阈值配置
    VolumeConfig     VolumeConfig     // 成交量验证配置
}
```

**默认配置**:
```go
SwingPointConfig: {
    LookbackPeriod: 5,        // 回看周期
    MinStrength: 0.5,         // 最小强度
    FractalReduction: 0.7,    // 分形强度打折
}

TrendLineConfig: {
    MinTouches: 2,            // 最小触及次数
    MaxDistance: 0.02,        // 2%容忍度
    MinSlope: 0.0001,
}

ThresholdConfig: {
    DefaultThreshold: 0.02,   // 2% (替代硬编码的8%)
    ATRMultiplier: 1.5,       // ATR倍数
    ATRPeriod: 14,
}

VolumeConfig: {
    WeightInStrength: 0.5,    // 成交量在强度中的权重
    WeightInQuality: 0.4,     // 成交量在质量中的权重
    SupportThreshold: 40.0,   // 支撑阈值
    PenaltyMultiplier: 0.7,   // 不足时的惩罚系数
}
```

---

## 三、核心算法详解

### 3.1 摆动点识别

#### 确认摆动点识别 (identifySwingPoints)

**位置**: `market/dow_theory.go:76-110`

**两层识别机制**:

**1. 确认摆动点** (已确认，需要未来数据确认)
```go
lookback := 5  // 左右各5根K线

for i := lookback; i < len(klines)-lookback; i++ {
    // 高点判断
    if isSwingHigh(klines, i, lookback) {
        strength := calculateSwingPointStrength(klines, i, SwingHigh)
        if strength >= MinStrength {
            swingPoints = append(swingPoints, &SwingPoint{
                Type: SwingHigh,
                Price: klines[i].High,
                Time: klines[i].Time,
                Index: i,
                Strength: strength,
                Confirmed: true,
            })
        }
    }

    // 低点判断
    if isSwingLow(klines, i, lookback) {
        // 类似高点处理
    }
}
```

**判断条件**:
- 高点(SwingHigh): 中间K线的高点 > 左右各5根的所有高点
- 低点(SwingLow): 中间K线的低点 < 左右各5根的所有低点
- 强度筛选: 必须 ≥ MinStrength(0.5)

---

**2. 实时分形检测** (identifyRealtimeFractals)

**位置**: `market/dow_theory.go:174-192`

**William's Fractal算法** (解决未来函数问题):
```go
// 分形高点检测
func isFractalHigh(klines []Kline, index int) bool {
    centerHigh := klines[index].High

    // 只需检查左右各2根K线
    for i := index - 2; i <= index + 2; i++ {
        if i == index { continue }
        if klines[i].High >= centerHigh {
            return false  // 不满足条件
        }
    }
    return true  // 满足分形高点条件
}
```

**特点**:
- 只需左右各2根K线
- 适用于实时行情，无需等待未来数据
- 强度降低系数: 0.7（标记为未确认）
- 打折系数: 0.8（降低风险）

---

#### 摆动点强度评估 (calculateSwingPointStrength)

**位置**: `market/dow_theory.go:257-348`

**评分公式**:
```
强度 = 价格范围权重 × (1 - VolumeWeight) + 成交量权重 × VolumeWeight
```

**详细计算**:

```go
func calculateSwingPointStrength(klines, index, swingType) float64 {
    // 1. 价格范围评分
    lookbackRange := 10
    priceRange := calculatePriceRange(klines, index, lookbackRange)

    // 高点强度 = (中点高价 - 最低价) / (最高价 - 最低价)
    // 低点强度 = (最高价 - 中点低价) / (最高价 - 最低价)

    // 2. 成交量权重
    volumeLookback := 20
    avgVolume := calculateAverageVolume(klines, index, volumeLookback)
    currentVolume := klines[index].Volume
    volumeWeight := currentVolume / avgVolume
    volumeWeight = min(volumeWeight, 3.0)  // 上限3倍

    // 3. 综合计算
    priceWeight := 1.0 - config.WeightInStrength  // 0.5
    volumeWeightRatio := config.WeightInStrength  // 0.5

    strength := priceRange × priceWeight + volumeWeight × volumeWeightRatio
    strength = min(strength, 10.0)  // 最大值10

    return strength
}
```

---

### 3.2 趋势线计算

#### 趋势线构建 (calculateTrendLines)

**位置**: `market/dow_theory.go:391-435`

**流程**:
```
1. 从摆动点中找到趋势线
   └─ findTrendLinesFromPoints()
      ├─ 分离高点和低点
      ├─ 对每个点对计算趋势线参数
      └─ 计算触及次数

2. 线性回归计算参数
   slope = (point2.Price - point1.Price) / (point2.Time - point1.Time)
   intercept = point1.Price - slope × point1.Time

3. 触及次数统计
   └─ countTrendLineTouches()
      maxDistance = 0.02  // 2%容忍度
      for each point:
          expectedPrice = slope × point.Time + intercept
          distance = |point.Price - expectedPrice| / point.Price
          if distance <= maxDistance:
              touches++

4. 强度评分
   └─ calculateTrendLineStrength()
```

---

#### 趋势线强度评分 (calculateTrendLineStrength)

**位置**: `market/dow_theory.go:437-467`

**评分公式**:
```go
strength := 0.0

// 1. 基础强度：触及次数
strength += float64(touches) × 1.0

// 2. 时间跨度
timeSpanDays := (endTime - startTime) / (24 * 3600 * 1000)
strength += min(timeSpanDays / 10, 2.0)

// 3. 摆动点强度
avgPointStrength := Σ point.Strength / len(points)
strength += avgPointStrength × 0.5

// 4. 角度奖励
angle := calculateAngle(slope)
if angle >= 15 && angle <= 75 {
    strength += 0.5
}

return strength
```

**强度阈值**: MinTouches ≥ 2（最少触及2次）

---

### 3.3 趋势方向判断

#### determineTrendDirection - 四层评估机制

**位置**: `market/dow_theory.go:1171-1261`

**评估层次**:

**1. 价格趋势分析 (权重 30%)**
```go
priceChange := (最新收盘 - N根K线前开盘) / N根前开盘

priceTrend := 0.0
if priceChange > threshold {
    priceTrend = 1.0  // 上升
} else if priceChange < -threshold {
    priceTrend = -1.0  // 下降
}
```

**2. 摆动点道氏分析 (权重 50%)** ⭐ 核心逻辑
```go
// 要求至少6个摆动点
if len(swingPoints) < 6 {
    return "flat"
}

// 取最近6个，分离为高点组和低点组
recentHighs := filter(swingPoints, Type == "high")
recentLows := filter(swingPoints, Type == "low")

// 高点趋势判断
highTrend := 0.0
for i := 1; i < len(recentHighs); i++ {
    if recentHighs[i].Price > recentHighs[i-1].Price {
        highTrend += 1.0  // 高点上升
    } else {
        highTrend -= 1.0  // 高点下跌
    }
}

// 低点趋势判断
lowTrend := 0.0
for i := 1; i < len(recentLows); i++ {
    if recentLows[i].Price > recentLows[i-1].Price {
        lowTrend += 1.0   // 低点上升
    } else {
        lowTrend -= 1.0   // 低点下跌
    }
}

// 道氏理论标准：高点和低点都要同向
swingDirection := 0.0
if highTrend > 0 && lowTrend > 0 {
    swingDirection = 1.0   // 明确上升趋势
} else if highTrend < 0 && lowTrend < 0 {
    swingDirection = -1.0  // 明确下降趋势
}
```

**3. 移动平均线确认 (权重 20%)**
```go
maTrend := 0.0

// 上升：Close > MA20 > MA50
if close > ma20 && ma20 > ma50 {
    maTrend = 1.0
}

// 下降：Close < MA20 < MA50
if close < ma20 && ma20 < ma50 {
    maTrend = -1.0
}
```

**4. 动态阈值** (新增)
```go
// 基于ATR自适应计算趋势门槛值
threshold := calculateDynamicThreshold(klines)
// 避免硬编码的8%固定阈值
```

**综合判断**:
```go
totalScore := priceTrend × 0.3 + swingDirection × 0.5 + maTrend × 0.2

if totalScore > 0.3 {
    return "up"
} else if totalScore < -0.3 {
    return "down"
} else {
    return "flat"
}
```

---

### 3.4 趋势强度评估

#### assessTrendStrength - 多维度评分体系

**位置**: `market/dow_theory.go:770-821`

**评分公式**:
```
整体强度(Overall) = 短期强度(40%) + 长期强度(60%) × 成交量惩罚系数
```

**短期强度计算** (基于5m数据，最近30根)

**位置**: `market/dow_theory.go:823-881`

```go
func calculateShortTermStrength(klines5m) float64 {
    // 1. 价格动量
    priceChange := (最新收盘 - 开盘) / 开盘
    momentumScore := priceChange × 500

    // 2. MA趋势
    maTrendScore := 0.0
    if close > ma20 && ma20 > ma50 {
        maTrendScore = 25  // 多头排列
    } else if close < ma20 && ma20 < ma50 {
        maTrendScore = -25  // 空头排列
    }

    // 3. 波动性评分
    volatility := calculateVolatility(klines5m)
    volatilityScore := 1.0 - volatility × 10
    volatilityScore = max(0, min(volatilityScore, 1)) × 100

    // 4. 相对强度
    recent5Avg := average(klines5m[25:30])
    prev5Avg := average(klines5m[20:25])
    relativeStrength := (recent5Avg - prev5Avg) / prev5Avg × 100

    rawStrength := momentumScore + maTrendScore + volatilityScore + relativeStrength

    // 5. 映射到[0,100]
    neutralPoint := 40.0
    maxRange := 85.0

    if rawStrength >= 0 {
        return min(40 + (rawStrength/100)×60, 100)  // 正值映射到[40,100]
    } else {
        return max(40 + (rawStrength/85)×40, 0)     // 负值映射到[0,40]
    }
}
```

**长期强度计算** (基于4h数据，最近30根)

**位置**: `market/dow_theory.go:903-963`

```go
func calculateLongTermStrength(klines4h) float64 {
    // 1. 百分比斜率（使用对数价格避免资产歧视）
    logPrices := []float64{}
    for _, k := range klines4h {
        logPrices = append(logPrices, math.Log(k.Close))
    }

    slope, rSquared := linearRegression(logPrices)
    slopeScore := slope × 1000  // 百分比斜率

    // 2. R²值评分（趋势线性度）
    rSquaredScore := rSquared × 40

    // 3. MA位置
    maScore := 0.0
    if ema20 > ema50 {
        maScore = 30  // 上升趋势
    } else if ema20 < ema50 {
        maScore = -30  // 下降趋势
    }

    rawStrength := slopeScore + rSquaredScore + maScore

    // 映射到[0,100]
    return mapToRange(rawStrength, 0, 100)
}
```

**动量评分** (Momentum)

```go
func calculateMomentum(klines) float64 {
    // 1. ROC (Rate of Change)
    roc := (close - close_20bars_ago) / close_20bars_ago
    rocScore := roc × 30

    // 2. RSI相对于50中点的偏离
    rsi14 := calculateRSI(klines, 14)
    rsiScore := (rsi14 - 50) × 1.4

    // 3. MACD绝对值
    macd := calculateMACD(klines)
    macdScore := abs(macd) × 20

    momentum := rocScore + rsiScore + macdScore
    return clamp(momentum, 0, 100)
}
```

**一致性评分** (Consistency)

```go
func calculateConsistency(shortTermChange, longTermChange) float64 {
    if sign(shortTermChange) == sign(longTermChange) {
        // 一致：短期和长期同向
        consistency := 100 - abs(shortTermChange - longTermChange) × 100
    } else {
        // 不一致：短期和长期反向
        consistency := 100 - (abs(shortTermChange) + abs(longTermChange)) × 100
    }

    return max(0, min(consistency, 100))
}
```

**成交量支撑度** (VolumeSupport)

```go
func calculateVolumeSupport(klines) float64 {
    recent5Volume := average(klines[len-5:])
    historical15Volume := average(klines[len-20:len-5])

    ratio := recent5Volume / historical15Volume

    if ratio > 1.5 {
        return 100  // 强支撑
    } else if ratio > 1.2 {
        return 75   // 较强支撑
    } else if ratio > 0.8 {
        return 50   // 一般支撑
    } else {
        return 25   // 弱支撑
    }
}
```

**成交量惩罚机制**:
```go
if volumeSupport < 40 {
    overallStrength *= 0.7  // 惩罚系数
}
```

---

### 3.5 交易信号生成

#### generateTradingSignal - 三层信号优先级

**位置**: `market/dow_theory.go:1373-1405`

**优先级1: 趋势跟随信号** (最高优先级)
```go
// 要求：趋势强度 > 75 AND 一致性 > 80%
if trendStrength.Overall > 75 && trendStrength.Consistency > 80 {
    if trendStrength.Direction == "up" {
        signal = TradingSignal{
            Action: "Buy",
            Confidence: min(trendStrength.Overall × 0.9, 95),
            Reasoning: "强上升趋势，高一致性",
        }
    } else if trendStrength.Direction == "down" {
        signal = TradingSignal{
            Action: "Sell",
            Confidence: min(trendStrength.Overall × 0.9, 95),
            Reasoning: "强下降趋势，高一致性",
        }
    }
    return signal
}
```

**优先级2: 突破信号** (中等优先级)
```go
// 检查是否突破重要趋势线
for _, trendLine := range trendLines {
    if trendLine.Strength > 3.0 && !trendLine.Broken {
        breakoutDistance := abs(currentPrice - expectedPrice) / currentPrice

        if breakoutDistance > 0.015 {  // 突破幅度 > 1.5%
            if trendLine.Type == "support" && currentPrice < expectedPrice {
                signal = TradingSignal{
                    Action: "Sell",
                    Confidence: 70,
                    Reasoning: "突破支撑线",
                }
            } else if trendLine.Type == "resistance" && currentPrice > expectedPrice {
                signal = TradingSignal{
                    Action: "Buy",
                    Confidence: 70,
                    Reasoning: "突破阻力线",
                }
            }
            return signal
        }
    }
}
```

**优先级3: 默认持有信号**
```go
signal = TradingSignal{
    Action: "Hold",
    Confidence: 30,
    Reasoning: "无明确信号",
}
```

---

## 四、主分析流程

### Analyze - 主入口函数

**签名**: `func (dta *DowTheoryAnalyzer) Analyze(klines5m, klines4h []Kline, currentPrice float64) *DowTheoryData`

**流程图**:
```
输入：
  - klines5m: 5分钟K线数据 (最少50根，建议200根)
  - klines4h: 4小时K线数据 (最少100根，建议500根)
  - currentPrice: 当前价格

处理步骤：
  1. 数据量校验
     └─ 5m >= 50根, 4h >= 100根

  2. identifySwingPoints(4h数据)
     └─ 识别摆动点（确认 + 实时分形）

  3. calculateTrendLines(摆动点)
     └─ 构建趋势线
     └─ 计算触及次数和强度

  4. assessTrendStrength(5m + 4h数据)
     └─ 计算短期强度
     └─ 计算长期强度
     └─ 计算动量、一致性、成交量支撑

  5. generateTradingSignal(短期K线 + 趋势强度)
     └─ 生成交易信号

输出：
  DowTheoryData{
      SwingPoints,    // 摆动点列表
      TrendLines,     // 趋势线列表
      TrendStrength,  // 趋势强度评估
      TradingSignal,  // 交易信号
  }
```

---

## 五、关键算法特点

### 5.1 修复的问题

**1. 未来函数问题** ✅
- 使用分形检测(William's Fractal)替代需要未来数据的确认逻辑
- 只需检查左右各2根K线，可实时应用

**2. "资产歧视"漏洞** ✅
- 使用对数价格计算百分比斜率
- `calculatePercentageTrendSlope()` 采用ln(P)回归
- 避免BTC/山寨币一刀切

**3. 成交量权重不足** ✅
- WeightInStrength从30%提升到50%
- WeightInQuality从33%提升到40%
- 引入PenaltyMultiplier(0.7)对低成交量进行惩罚

**4. 动态阈值** ✅
- 替代硬编码的8%趋势判断阈值
- 基于ATR自适应计算(1.5倍ATR)
- 范围限制：0.5% ~ 5%

---

### 5.2 高级特性

**1. 多时间框架对齐**
- 短期(5m)和长期(4h)分别计算强度
- 一致性评分反映两者协调度

**2. 成交量确认机制**
```go
func confirmWithVolume(volumeRatio float64) bool {
    if volumeRatio > 2.0 {
        return true  // 强确认
    } else if volumeRatio > 1.5 {
        return true  // 中等确认
    } else if volumeRatio > 1.2 {
        return true  // 一般确认
    }
    return false
}
```

**3. 风险收益比评估**
```go
func calculateRiskReward(signal TradingSignal) float64 {
    stopLoss := calculateStopLoss(trendLine)
    takeProfit := calculateTakeProfit(trendLine)
    entry := currentPrice

    risk := abs(entry - stopLoss)
    reward := abs(takeProfit - entry)

    riskRewardRatio := reward / risk

    if riskRewardRatio < config.RiskRewardMin {  // 1.5
        signal.Confidence *= 0.7  // 降低置信度
    }

    return riskRewardRatio
}
```

**4. 支持向后兼容**
```go
// 配置可动态修改
func GetDowTheoryConfig() DowTheoryConfig
func UpdateDowTheoryConfig(config DowTheoryConfig)
```

---

## 六、辅助指标计算

### 在道氏理论分析中使用的辅助指标

| 指标 | 周期 | 用途 | 计算方式 |
|------|------|------|---------|
| **EMA** | 20/50 | MA趋势确认 | 指数移动平均 |
| **RSI** | 7/14 | 动量评估 | Wilder平滑法 |
| **MACD** | 12/26 | 趋势动量 | EMA12 - EMA26 |
| **ATR** | 14 | 动态阈值 | 平均真实波幅 |
| **R²** | - | 趋势线性度 | 1 - (残差平方和/总平方和) |

**计算位置**:
- `calculateRSI()` - data.go:372-419
- `calculateMACD()` - data.go:352-369
- `calculateATR()` - dow_theory.go:1136-1169
- `calculateRSquared()` - dow_theory.go:1075-1111

---

## 七、实际应用流程图

```
市场数据获取
    ↓
[5m K线] ────→ 短期强度计算
    ↓                ↓
[4h K线] ──→ 摆动点识别 ──→ 趋势线构建
    ↓                ↓            ↓
[当前价格] ──→ 长期强度计算 ──→ 趋势方向判断
    ↓                              ↓
成交量确认 ←──────────────────────┘
    ↓
动量+一致性+成交量综合评估
    ↓
交易信号生成(趋势跟随 > 突破 > 持有)
    ↓
风险收益比评估
    ↓
DowTheoryData输出
```

---

## 八、关键源代码位置

| 功能模块 | 函数 | 行号 |
|---------|------|------|
| 摆动点识别 | `identifySwingPoints` | 67-117 |
| 分形检测 | `identifyRealtimeFractals` | 120-171 |
| 强度计算 | `calculateSwingPointStrength` | 257-348 |
| 趋势线构建 | `calculateTrendLines` | 351-388 |
| 趋势判断 | `determineTrendDirection` | 1171-1261 |
| 强度评估 | `assessTrendStrength` | 770-821 |
| 短期强度 | `calculateShortTermStrength` | 823-881 |
| 长期强度 | `calculateLongTermStrength` | 903-963 |
| 交易信号 | `generateTradingSignal` | 1373-1405 |
| 风险收益 | `calculateRiskReward` | 1651-1672 |

---

## 九、使用示例

### 9.1 基本分析

```go
// 创建分析器
analyzer := NewDowTheoryAnalyzer()

// 获取K线数据
klines5m := getKlines("BTCUSDT", "5m", 200)
klines4h := getKlines("BTCUSDT", "4h", 500)
currentPrice := 45000.0

// 执行分析
result := analyzer.Analyze(klines5m, klines4h, currentPrice)

// 查看结果
fmt.Printf("摆动点数量: %d\n", len(result.SwingPoints))
fmt.Printf("趋势线数量: %d\n", len(result.TrendLines))
fmt.Printf("趋势方向: %s\n", result.TrendStrength.Direction)
fmt.Printf("整体强度: %.1f\n", result.TrendStrength.Overall)
fmt.Printf("交易信号: %s (置信度: %.1f%%)\n",
    result.TradingSignal.Action, result.TradingSignal.Confidence)
```

### 9.2 自定义配置

```go
// 修改配置
config := GetDowTheoryConfig()
config.SwingPointConfig.LookbackPeriod = 7
config.TrendLineConfig.MinTouches = 3
config.VolumeConfig.WeightInStrength = 0.6
UpdateDowTheoryConfig(config)

// 使用新配置分析
result := analyzer.Analyze(klines5m, klines4h, currentPrice)
```

### 9.3 详细信息查看

```go
// 查看摆动点详情
for i, sp := range result.SwingPoints {
    fmt.Printf("摆动点 %d: 类型=%s, 价格=%.2f, 强度=%.2f, 确认=%v\n",
        i, sp.Type, sp.Price, sp.Strength, sp.Confirmed)
}

// 查看趋势线详情
for i, tl := range result.TrendLines {
    fmt.Printf("趋势线 %d: 类型=%s, 触及=%d, 强度=%.2f, 突破=%v\n",
        i, tl.Type, tl.Touches, tl.Strength, tl.Broken)
}

// 查看详细强度信息
ts := result.TrendStrength
fmt.Printf("短期强度: %.1f\n", ts.ShortTerm)
fmt.Printf("长期强度: %.1f\n", ts.LongTerm)
fmt.Printf("动量: %.1f\n", ts.Momentum)
fmt.Printf("一致性: %.1f%%\n", ts.Consistency)
fmt.Printf("成交量支撑: %.1f\n", ts.VolumeSupport)
```

---

## 十、常见问题

### 10.1 为什么摆动点数量少？

**可能原因**:
1. K线数据不足（需要至少50-100根4h K线）
2. MinStrength设置过高（默认0.5）
3. 市场处于低波动期，缺少明显转折点

**解决方案**:
```go
config := GetDowTheoryConfig()
config.SwingPointConfig.MinStrength = 0.3  // 降低强度阈值
UpdateDowTheoryConfig(config)
```

### 10.2 为什么趋势强度为0？

**可能原因**:
1. 成交量支撑度过低（<40），触发惩罚机制
2. 短期和长期趋势不一致
3. 价格波动过大，影响评分

**解决方案**:
- 检查成交量数据是否正常
- 查看短期和长期强度的具体数值
- 调整VolumeConfig.SupportThreshold

### 10.3 为什么总是返回"Hold"信号？

**可能原因**:
1. 趋势强度不足75（趋势跟随条件）
2. 一致性不足80%
3. 没有检测到有效的趋势线突破

**这是正常的**: 道氏理论是保守的分析方法，只在高确信度时才给出明确信号。

---

## 十一、总结

该道氏理论实现是一个**生产级的技术分析引擎**，特点包括：

✅ **严格的道氏理论原理**: 高低点同向确认趋势
✅ **多维度评估**: 强度、方向、质量、一致性、动量、成交量
✅ **自适应机制**: 动态阈值、时间框架对齐
✅ **风险控制**: 成交量惩罚、风险收益比筛选
✅ **实时可用**: 分形检测避免未来函数问题
✅ **可配置性**: 完整的配置体系，支持动态调整

适用于中短期趋势跟随策略，尤其在高波动市场中表现稳定。

---

**文档版本**: 1.0
**最后更新**: 2026-01-18
**维护者**: AI自动交易系统开发团队
