# 供需区相关代码完整文档

## 概述

本文档详细记录了AI自动交易系统中供需区(Supply and Demand Zones)相关的逻辑代码，包括区域识别、强度评估、状态管理等核心算法。供需区分析是Smart Money概念的核心，为AI提供机构交易者关注的关键价格区域。

---

## 一、核心文件列表

### 1.1 主要供需区文件

| 文件路径 | 行数 | 主要功能 |
|---------|------|---------|
| `market/supply_demand.go` | 2167 | 供需区识别和分析引擎 |
| `market/supply_demand_context.go` | 96 | 上下文评分计算 |
| `market/types.go` | 900+ | 数据结构定义 |
| `market/context_scoring.go` | 513 | 通用上下文计算器 |
| `market/data_cleaner.go` | 300+ | 数据清洗模块 |
| `market/strength_normalizer.go` | 1900+ | 强度归一化，Z-Score计算 |

### 1.2 集成文件

- `market/comprehensive_analysis.go` - 多时间框架综合分析
- `market/anchor_adapters.go` - 锚点转换适配器
- `market/atr_manager.go` - ATR管理（用于动态阈值）

---

## 二、数据结构定义

### 2.1 核心数据结构

#### SupplyDemandZone - 供需区主结构
```go
type SupplyDemandZone struct {
    ID            string                 // 区域唯一标识
    Type          ZoneType               // "supply" 或 "demand"
    UpperBound    float64                // 上边界价格
    LowerBound    float64                // 下边界价格
    CenterPrice   float64                // 中心价格 = (High+Low)/2
    Width         float64                // 区域宽度（绝对值）
    WidthPercent  float64                // 区域宽度百分比

    // 强度与质量评估
    Strength      float64                // 原始强度分数 (0-100)
    StrengthZ     float64                // Z-Score标准化强度
    StrengthZReady bool                  // 是否有足够样本
    SampleCount   int                    // 参与标准化的样本数
    Quality       ZoneQuality            // "strong"/"good"/"moderate"/"weak"
    VolatilityGrade VolatilityGrade      // ATR评级: A/B/C/D

    // 状态追踪
    Status        ZoneStatus             // "fresh"/"tested"/"testing"/"weakened"/"broken"/"expired"
    IsActive      bool                   // 是否活跃
    IsBroken      bool                   // 是否被突破
    TouchCount    int                    // 触及次数（最多8次）
    LastTouch     int64                  // 最后触及时间
    CreationTime  int64                  // 创建时间(毫秒)
    BreakTime     int64                  // 突破时间

    // 起源信息
    Origin *ZoneOrigin {
        KlineIndex    int                // K线索引
        PatternType   PatternType        // 识别模式
        ImpulseMove   float64            // 冲击幅度%
        ImpulseVolume float64            // 冲击成交量
        TimeFrame     string             // 时间框架
        Confirmation  bool               // 是否确认
    }

    // 成交量分析
    Volume        float64                // 相关成交量
    VolumeProfile *ZoneVP {
        TotalVolume    float64            // 总成交量
        BuyVolume      float64            // 买入量
        SellVolume     float64            // 卖出量
        VolumeImbalance float64          // 买卖比
    }

    // 上下文评分
    Context *ContextMetrics {
        StrengthZ   float64              // 强度标准分 (>1.5为强)
        WidthATR    float64              // 宽度相对ATR倍数
        VolRatio    float64              // 成交量倍数
        IsFresh     bool                 // 是否新鲜
        TimeScore   float64              // 时间衰减评分(0-1)
        RankPct     float64              // 排名百分位(0-1)
    }

    // 模糊逻辑缓冲
    FuzzyTolerance      float64          // 缓冲区大小
    MaxPenetrationPct   float64          // 最大穿透深度%
    DeepTouchCount      int              // 深度触及次数
    LastPenetrationPct  float64          // 最近穿透深度%
}
```

---

### 2.2 区域类型枚举

#### ZoneType - 区域类型
```go
type ZoneType string

const (
    SupplyZone  ZoneType = "supply"    // 供给区 (压力位/阻力位)
    DemandZone  ZoneType = "demand"    // 需求区 (支撑位)
)
```

---

#### ZoneStatus - 区域状态
```go
type ZoneStatus string

const (
    StatusFresh     = "fresh"          // 新鲜：未触碰
    StatusTested    = "tested"         // 已测试：已触碰但未破坏
    StatusTesting   = "testing"        // 正在测试：刺破但未达突破阈值(SFP/假突破)
    StatusWeakened  = "weakened"       // 已弱化：触碰>3次或深度穿透>50%
    StatusBroken    = "broken"         // 已突破：失效
    StatusExpired   = "expired"        // 已过期：时间过长失效
)
```

---

#### ZoneQuality - 区域质量
```go
type ZoneQuality string

const (
    QualityStrong   = "strong"         // 强 (score >= 80)
    QualityGood     = "good"           // 良好 (score >= 65)
    QualityModerate = "moderate"       // 中等 (score >= 50)
    QualityWeak     = "weak"           // 弱 (score < 50)
)
```

---

#### VolatilityGrade - 波动率评级
```go
type VolatilityGrade string

const (
    VolGradeA = "A"  // 最优 (0.2 < width_atr < 1.5)
    VolGradeB = "B"  // 可交易 (1.5 <= width_atr <= 2.0)
    VolGradeC = "C"  // 谨慎 (0.15-0.2 或 2.0-3.0)
    VolGradeD = "D"  // 不建议 (width_atr < 0.15 或 > 3.0)
)
```

---

#### PatternType - 识别模式
```go
type PatternType string

const (
    RallyBaseRally   = "rally_base_rally"   // 上涨-整理-上涨 (需求区)
    DropBaseDrop     = "drop_base_drop"     // 下跌-整理-下跌 (供给区)
    RallyBaseDropOB  = "rally_base_drop_ob" // 上涨-整理-下跌 (订单区块)
    DropBaseRallyOB  = "drop_base_rally_ob" // 下跌-整理-上涨 (订单区块)
    FreshSupply      = "fresh_supply"       // 新鲜供给区
    FreshDemand      = "fresh_demand"       // 新鲜需求区
)
```

---

### 2.3 分析结果容器

#### SupplyDemandData - 供需区分析结果
```go
type SupplyDemandData struct {
    SupplyZones  []*SupplyDemandZone    // 所有供给区
    DemandZones  []*SupplyDemandZone    // 所有需求区
    ActiveZones  []*SupplyDemandZone    // 活跃区域 (动态过滤)
    Config       *SDConfig              // 分析配置
    Statistics   *SDStatistics          // 统计信息
    LastAnalysis int64                  // 最后分析时间
}
```

#### SDStatistics - 统计信息
```go
type SDStatistics struct {
    TotalSupplyZones   int      // 总供给区数
    TotalDemandZones   int      // 总需求区数
    ActiveSupplyZones  int      // 活跃供给区数
    ActiveDemandZones  int      // 活跃需求区数
    AvgZoneStrength    float64  // 平均区域强度
    AvgZoneWidth       float64  // 平均区域宽度%
    SuccessRate        float64  // 成功率 (有反应的比例)
    BreakoutRate       float64  // 突破率
    ReactionRate       float64  // 反应率
}
```

---

## 三、供需区识别算法

### 3.1 识别流程

#### AnalyzeWithSymbol - 主入口函数

**流程**:
```
1. 数据验证 (最少10根K线)
   └─ 返回空结构体避免null输出

2. 供给区识别 (identifySupplyZones)
   ├─ Drop-Base-Drop模式
   ├─ Rally-Base-Drop模式 (订单区块)
   └─ 新鲜供给区

3. 需求区识别 (identifyDemandZones)
   ├─ Rally-Base-Rally模式
   ├─ Drop-Base-Rally模式 (订单区块)
   └─ 新鲜需求区

4. 强度计算与排序
   └─ 按强度排序 → 过滤重叠区域 → 保留最强的
      (overlap_ratio阈值: 70%)

5. 备用识别机制 (P0修复)
   └─ 如果供给区<2或需求区<2 → 启用基础高低点识别
      ├─ 基础供给区 = 最近50K线的最高点
      └─ 基础需求区 = 最近50K线的最低点

6. 上下文评分
   └─ 为每个区域计算ContextMetrics
      包含：StrengthZ, WidthATR, VolRatio, IsFresh, TimeScore, RankPct

7. 数据清洗 (P2修复)
   └─ 清洗异常的width_atr和vol_ratio值
      模式：soft(限幅) 或 hard(过滤)

8. 状态更新 (updateZoneStatuses)
   └─ 检查区域年龄 → 分析价格交互 → 计算触及次数 → 验证区域反应

输出: SupplyDemandData
```

---

### 3.2 Drop-Base-Drop模式识别

**函数**: `identifyDropBaseDrop(klines, atr, symbol, timeframe)`

**算法步骤**:

**步骤1: 寻找整理区域 (Base Area)**
```go
func findBaseArea(klines, centerIndex, atr) (baseStart, baseEnd int) {
    // 使用ATR归一化替代硬编码百分比
    baseRangeThreshold := 1.0 × atr  // k=1.0倍ATR作为Base范围

    maxLookback := 8  // 向左最多看8根K线

    // 向左扩展
    currentRange := 0.0
    for i := centerIndex - 1; i >= centerIndex-maxLookback; i-- {
        currentRange = klines[centerIndex].High - klines[i].Low
        if currentRange <= baseRangeThreshold {
            baseStart = i
        } else {
            break  // 遇到大波动停止
        }
    }

    // 向右扩展
    for i := centerIndex + 1; i <= centerIndex+maxLookback; i++ {
        currentRange = klines[i].High - klines[baseStart].Low
        if currentRange <= baseRangeThreshold {
            baseEnd = i
        } else {
            break
        }
    }

    return baseStart, baseEnd
}
```

**特点**:
- 使用ATR归一化，避免硬编码百分比
- 最少需要2根K线形成Base区域
- 向左右各最多看8根K线

---

**步骤2: 验证左侧移动 (下跌)**
```go
func validateLeftMove(klines, dropStart, baseStart) (bool, float64, float64) {
    minChange := 0.003  // 0.3%最小冲击百分比

    // 计算下跌幅度
    dropMove := (klines[baseStart].Low - klines[dropStart].High) / klines[dropStart].High

    if dropMove < -minChange {  // 负值表示下跌
        // 计算冲击成交量
        impulseVolume := 0.0
        for i := dropStart; i < baseStart; i++ {
            impulseVolume += klines[i].Volume
        }

        return true, abs(dropMove), impulseVolume
    }

    return false, 0, 0
}
```

---

**步骤3: 验证右侧移动 (下跌)**
```go
func validateRightMove(klines, baseEnd, lookForward) (bool, float64) {
    minChange := 0.003  // 0.3%

    for i := baseEnd + 1; i <= baseEnd + lookForward; i++ {
        dropMove := (klines[i].Low - klines[baseEnd].High) / klines[baseEnd].High

        if dropMove < -minChange {
            return true, abs(dropMove)
        }
    }

    return false, 0
}
```

---

**步骤4: 创建供给区**
```go
func createSupplyZone(klines, baseStart, baseEnd, leftMove, rightMove, impulseVolume) *SupplyDemandZone {
    zone := &SupplyDemandZone{
        Type: SupplyZone,
        UpperBound: klines[baseEnd].High,
        LowerBound: klines[baseStart].Low,
        CenterPrice: (klines[baseEnd].High + klines[baseStart].Low) / 2,
        Width: klines[baseEnd].High - klines[baseStart].Low,
        WidthPercent: (klines[baseEnd].High - klines[baseStart].Low) / klines[baseStart].Low × 100,

        Origin: &ZoneOrigin{
            KlineIndex: baseStart,
            PatternType: DropBaseDrop,
            ImpulseMove: max(leftMove, rightMove),
            ImpulseVolume: impulseVolume,
            TimeFrame: timeframe,
            Confirmation: rightMove > 0,  // 右侧有移动即确认
        },

        Status: StatusFresh,
        IsActive: true,
        CreationTime: klines[baseEnd].Time,
    }

    return zone
}
```

---

### 3.3 Rally-Base-Rally模式识别

**对称逻辑**: 与Drop-Base-Drop完全对称，只是方向相反

**关键差异**:
- 左侧验证: 上涨 (价格上升)
- 右侧验证: 上涨 (价格上升)
- 区域类型: DemandZone
- Base区域: 使用Low而非High

---

### 3.4 新鲜供给/需求区识别

**函数**: `identifyFreshSupply(klines, atr)` / `identifyFreshDemand(klines, atr)`

**算法**:
```go
func identifyFreshSupply(klines, atr) []*SupplyDemandZone {
    zones := []*SupplyDemandZone{}
    minImpulsePercent := 0.003  // 0.3%

    for i := 1; i < len(klines); i++ {
        // 检测显著价格下跌
        priceMove := (klines[i].Close - klines[i-1].Close) / klines[i-1].Close

        if priceMove < -minImpulsePercent {
            // 创建新鲜供给区
            zone := &SupplyDemandZone{
                Type: SupplyZone,
                UpperBound: klines[i-1].High,
                LowerBound: klines[i-1].Low,
                CenterPrice: (klines[i-1].High + klines[i-1].Low) / 2,

                Origin: &ZoneOrigin{
                    KlineIndex: i-1,
                    PatternType: FreshSupply,
                    ImpulseMove: abs(priceMove),
                    ImpulseVolume: klines[i].Volume,
                },

                Status: StatusFresh,
                IsActive: true,
            }

            zones = append(zones, zone)
        }
    }

    return zones
}
```

---

### 3.5 强度计算

#### calculateZoneStrength - 综合强度评分

**公式**:
```
强度 = 冲击移动强度 + 成交量强度 + 区域宽度强度 + 模式类型基础分 + 基础分数
```

**详细计算**:
```go
func calculateZoneStrength(zone, avgVolume) float64 {
    strength := 0.0

    // 1. 冲击移动强度 (权重 30)
    impulseFactor := zone.Origin.ImpulseMove × 30
    strength += impulseFactor

    // 2. 成交量强度 (权重 8)
    volumeRatio := zone.Origin.ImpulseVolume / avgVolume
    volumeFactor := min(volumeRatio, 3.0) × 8
    strength += volumeFactor

    // 3. 区域宽度强度 (反向评分，越窄越好)
    // 0.1%-12%范围内: max(10-WidthPercent, 2)
    widthFactor := max(10 - zone.WidthPercent, 2)
    strength += widthFactor

    // 4. 模式类型基础分
    switch zone.Origin.PatternType {
    case DropBaseDrop, RallyBaseRally:
        strength += 20  // 经典模式
    case RallyBaseDropOB, DropBaseRallyOB:
        strength += 18  // 订单区块
    case FreshSupply, FreshDemand:
        strength += 15  // 新鲜区域
    }

    // 5. 基础分数
    strength += 25

    // 范围限制: 15 <= Strength <= 100
    strength = max(15, min(strength, 100))

    return strength
}
```

---

### 3.6 Z-Score标准化

**目的**: 相对强度对比，避免"矮子里拔将军"

**算法**:
```go
func calculateStrengthZ(zone, symbol, timeframe) float64 {
    // 与全局强度归一化器集成
    normalizer := GetGlobalStrengthNormalizer()

    // 创建区域记录
    zoneRecord := &ZoneStrengthRecord{
        Symbol:       symbol,
        Timeframe:    timeframe,
        RawScore:     zone.Strength,
        ZoneType:     string(zone.Type),
        PatternType:  string(zone.Origin.PatternType),
        TouchCount:   zone.TouchCount,
        VolumeRatio:  zone.Volume / avgVolume,
        WidthPercent: zone.WidthPercent,
    }

    // 获取Z-Score (样本>15个时有效)
    zScoreResult := normalizer.GetZScoreWithUpdate(symbol, timeframe, zone.Strength, zoneRecord)

    zone.StrengthZ = zScoreResult.ZScore
    zone.StrengthZReady = zScoreResult.SampleSize >= 15
    zone.SampleCount = zScoreResult.SampleSize

    return zScoreResult.ZScore
}
```

**Z-Score解读**:
- Z > 1.5: 强区域 (前30%)
- Z > 2.0: 极强区域 (前10%)
- Z < -1.0: 弱区域

---

## 四、强度评估系统

### 4.1 上下文评分

#### SupplyDemandContextScoring - 7个核心评分维度

**评分维度**:
```go
type ContextMetrics struct {
    // 1. 强度标准分 (strength_z)
    StrengthZ  float64  // Z-Score: >1.5为强, >2.0为极强

    // 2. 宽度与ATR关系 (width_atr)
    WidthATR   float64  // Width / ATR14
                        // A级: 0.2 < width_atr < 1.5 (最优)
                        // B级: 1.5 <= width_atr <= 2.0
                        // C级: 0.15-0.2 或 2.0-3.0
                        // D级: <0.15 或 >3.0

    // 3. 成交量比率 (vol_ratio)
    VolRatio   float64  // Volume / MedianVolume20
                        // >2.0: 异常放量
                        // >1.5: 确认放量
                        // <0.8: 萎缩成交量

    // 4. 新鲜度 (is_fresh)
    IsFresh    bool     // 年龄 <= maxZoneAge / 3

    // 5. 时间评分 (time_score)
    TimeScore  float64  // exp(-age / maxAge), 范围[0,1]
                        // 越新鲜评分越高

    // 6. 排名百分位 (rank_pct)
    RankPct    float64  // percentile(strength)
                        // 0.8+ 表示前20%
                        // 0.5 表示中位数
}
```

**计算方法**:
```go
func calculateSingleZoneContext(zone, allZones, contextCalc) *ContextMetrics {
    // 第1步：收集全部数据用于相对比较
    allStrengths := extractStrengths(allZones)
    allWidths := extractWidths(allZones)
    allVolumes := extractVolumes(allZones)

    // 第2步：计算7个评分
    strengthZ := contextCalc.CalculateStrengthZ(zone.Strength, allStrengths)
    widthATR := contextCalc.CalculateWidthATR(zone.Width)
    volRatio := contextCalc.CalculateVolumeRatio(zone.VolumeProfile.TotalVolume)
    isFresh := contextCalc.IsFresh(zone.CreationTime, maxAge)
    timeScore := contextCalc.CalculateTimeScore(zone.CreationTime, maxAge)
    rankPct := contextCalc.CalculateRankPercentile(zone.Strength, allStrengths)

    // 第3步：基于ATR计算波动率评级
    zone.VolatilityGrade = contextCalc.CalculateVolatilityGrade(widthATR)

    // 第4步：返回综合指标
    return &ContextMetrics{
        StrengthZ: strengthZ,
        WidthATR:  widthATR,
        VolRatio:  volRatio,
        IsFresh:   isFresh,
        TimeScore: timeScore,
        RankPct:   rankPct,
    }
}
```

---

### 4.2 质量评估

#### assessZoneQuality - 质量等级判定

**算法**:
```go
func assessZoneQuality(zone) ZoneQuality {
    score := zone.Strength

    // 成交量不平衡调整
    if zone.Type == SupplyZone {
        if zone.VolumeProfile.VolumeImbalance < 0.8 {  // 卖盘占优
            score += 10
        }
    } else if zone.Type == DemandZone {
        if zone.VolumeProfile.VolumeImbalance > 1.2 {  // 买盘占优
            score += 10
        }
    }

    // 确认状态调整
    if zone.Origin.Confirmation {
        score += 5
    }

    // 质量分类
    if score >= 80 {
        return QualityStrong
    } else if score >= 65 {
        return QualityGood
    } else if score >= 50 {
        return QualityModerate
    } else {
        return QualityWeak
    }
}
```

---

## 五、市场分析中的应用

### 5.1 价格交互分析

#### analyzeZoneInteraction - 3个交互状态

**算法**:
```go
func analyzeZoneInteraction(zone, currentPrice, atr) string {
    distance := abs(currentPrice - zone.CenterPrice) / zone.CenterPrice

    if distance > 0.01 {
        return "no_interaction"  // 距离 > 1% → 无交互
    }

    // 检查价格是否在区域内
    if currentPrice >= zone.LowerBound && currentPrice <= zone.UpperBound {
        return "testing"  // 价格在区域内 → 正在测试
    }

    // 检查刺破程度
    penetration := 0.0
    if zone.Type == DemandZone {
        penetration = zone.LowerBound - currentPrice
    } else {
        penetration = currentPrice - zone.UpperBound
    }

    if penetration <= 0.1 × atr {
        return "testing"  // 刺破 <= 0.1×ATR → 轻微刺破
    } else if penetration < 0.5 × atr {
        return "testing"  // 0.1×ATR < 刺破 < 0.5×ATR → 中等刺破
    } else {
        return "true_breakout"  // 刺破 >= 0.5×ATR → 真正的突破
    }
}
```

---

### 5.2 区域状态转换逻辑

#### isTrueBreakout - 真假突破判定

**核心修复**: 使用ATR缓冲区区分真假突破 (P0修复)

**算法**:
```go
func isTrueBreakout(zone, currentPrice, atr) bool {
    // 使用ATR缓冲区
    bufferDistance := 0.2 × atr  // 0.2倍ATR作为缓冲

    if zone.Type == DemandZone {
        // 需求区突破判定
        if currentPrice < (zone.LowerBound - bufferDistance) {
            return true  // 真突破
        } else {
            return false // 假突破/测试
        }
    } else {
        // 供给区突破判定
        if currentPrice > (zone.UpperBound + bufferDistance) {
            return true  // 真突破
        } else {
            return false // 假突破/测试
        }
    }
}
```

---

#### handleZoneBreakout - 区域转换处理

**Breaker概念**: 需求区被突破 → 转换为供给区

**算法**:
```go
func handleZoneBreakout(zone, currentPrice, atr) {
    if isTrueBreakout(zone, currentPrice, atr) {
        // 标记为已突破
        zone.IsBroken = true
        zone.Status = StatusBroken
        zone.BreakTime = time.Now().UnixMilli()

        // 类型转换处理 (Breaker区)
        if zone.Type == DemandZone {
            // 需求区被跌破 → 转换为供给区
            zone.Type = SupplyZone
        } else {
            // 供给区被突破 → 转换为需求区
            zone.Type = DemandZone
        }

        // 转换后处理
        zone.Status = StatusTested
        zone.TouchCount = 1  // 重置
        zone.IsActive = true  // 重新激活
        zone.Strength = min(zone.Strength × 1.2, 100)  // 提升20%, 上限100
        zone.Quality = QualityGood  // 设置为良好
    }
}
```

---

### 5.3 触及次数统计

#### countZoneTouches - 避免数值爆炸

**核心修复**: 检查完整历史生命周期，不截断数据 (修复幽灵支撑Bug)

**算法**:
```go
func countZoneTouches(zone, klines) int {
    count := 0
    lastTouchIndex := -999
    minGapBetweenTouches := 5  // 聚类：至少间隔5根K线
    maxTouchCount := 8         // 最多8次触及 (AI V-10.0规则)

    // 遍历所有K线（完整生命周期）
    for i, kline := range klines {
        // 跳过创建时间之前的K线
        if kline.Time < zone.CreationTime {
            continue
        }

        // 触及标准
        if kline.High >= zone.LowerBound && kline.Low <= zone.UpperBound {
            if i - lastTouchIndex >= minGapBetweenTouches {
                count++
                lastTouchIndex = i
                zone.LastTouch = kline.Time

                if count >= maxTouchCount {
                    break  // 达到最大触及次数
                }
            }
        }
    }

    return count
}
```

---

### 5.4 交易信号生成

#### GenerateSignals - 3类信号

**1. SDSignalZoneEntry - 进入信号**
```go
条件：价格接近区域 (距离 < 5%)，在区域外
动作：需求区↓→买, 供给区↑→卖

置信度 = zone.Strength × (1 - distance/0.05) × 0.7
if zone.Quality == QualityStrong → +8
```

**2. SDSignalZoneBounce - 反弹信号**
```go
条件：价格在区域内 (0%-100%)
动作：需求区→买, 供给区→卖

置信度 = zone.Strength × 0.8
if zone.Quality == QualityStrong → +10
if zone.Status == StatusFresh → +5
```

**3. SDSignalFreshZone - 新鲜区域信号**
```go
条件：新近创建的高质量区域
动作：基于区域类型的强信号

置信度 = zone.Strength × 0.9 + 15 (最高)
```

---

## 六、数据清洗机制 (P2级修复)

### 6.1 清洗配置

```go
type CleaningConfig struct {
    // width_atr异常值检测
    WidthATRMin:       0.15   // 过窄阈值
    WidthATRMax:       3.0    // 过宽阈值
    WidthATRIQRFactor: 1.5    // IQR异常系数

    // vol_ratio异常值检测
    VolRatioMin:       0.2    // 过低阈值
    VolRatioMax:       15.0   // 过高阈值
    VolRatioIQRFactor: 2.0    // IQR异常系数

    // 清洗模式
    Mode:              "soft"  // "soft"(限幅模式) 或 "hard"(过滤模式)

    // 其他
    EnableLogging:     true
    EnableIQRFilter:   true
    PreserveFactor:    0.8    // 至少保留80%数据
}
```

---

### 6.2 清洗流程

**soft模式（默认）**:
```
├─ 检测异常值 (IQR方法)
├─ 对异常值进行限幅而非删除
└─ 保留数据完整性，修正异常
```

**hard模式**:
```
├─ 检测异常值
├─ 直接删除超过阈值的区域
└─ 可能导致数据损失 >20%
```

**异常值统计返回**:
```go
type CleaningStats struct {
    TotalZones       int              // 原始区域数
    FilteredZones    int              // 过滤后数量
    WidthATROutliers int              // width_atr异常数
    VolRatioOutliers int              // vol_ratio异常数
    FilterRate       float64          // 过滤率%
    QualityScore     float64          // 清洗后质量评分

    // 分桶统计（透明化）
    FilterReasonBuckets map[string]int  // "width_atr_low"等
    ClampedZones        int             // soft模式限幅数
    ClampedFieldsCount  int             // 限幅字段总数
}
```

---

## 七、配置参数详解

### 默认配置

```go
var defaultSDConfig = SDConfig{
    MinImpulsePercent:  0.003,   // 0.3% 最小冲击幅度
    MinBasePercent:     0.001,   // 0.1% 最小整理幅度
    MaxBasePercent:     0.12,    // 12% 最大整理幅度
    MinVolumeFactor:    0.8,     // 0.8倍 最小成交量确认
    MaxZoneAge:         200,     // 200根K线 最大存活期
    MaxTouchCount:      8,       // 8次 最大触及次数
    BreakoutThreshold:  0.02,    // 2% 突破阈值
    ConfirmationBars:   1,       // 1根 确认K线
    TimeFrames:         []{"5m", "15m", "30m", "1h", "4h"},
    EnableValidation:   false,   // 暂时关闭验证
    QualityThreshold:   0.15,    // 15% 质量阈值
}
```

---

## 八、核心优化与修复

| 修复级别 | 描述 | 影响 |
|---------|------|------|
| **P0-1** | 先计算强度后过滤重叠 | 确定性排序，避免强度依赖循环 |
| **P0-2** | 引入overlap_ratio=0.7 | 允许层级结构并存，避免过度互斥 |
| **P0-3** | K线数据缓存 | 同一请求内数据一致性 |
| **P0-4** | 动态VPVR配置 | 适应不同币种与时间框架 |
| **P0-5** | 统一last_price字段 | V-13.5规范兼容 |
| **P1-1** | ATR用于Base区识别 | 自适应波动率，跨币种统一 |
| **P1-2** | 距离到最近边界验证 | 保留边界贴近但中心较远的区域 |
| **P2** | soft/hard双模式清洗 | 平衡数据质量与完整性 |

---

## 九、使用示例

### 9.1 基本分析

```go
// 创建分析器
analyzer := NewSupplyDemandAnalyzer()

// 获取K线数据
klines := getKlines("BTCUSDT", "5m", 200)

// 执行分析
sdData := analyzer.AnalyzeWithSymbol(klines, "BTCUSDT", "5m")

// 查看结果
fmt.Printf("供给区数量: %d\n", len(sdData.SupplyZones))
fmt.Printf("需求区数量: %d\n", len(sdData.DemandZones))
fmt.Printf("活跃区域数量: %d\n", len(sdData.ActiveZones))

// 查看活跃区域详情
for _, zone := range sdData.ActiveZones {
    fmt.Printf("区域: %s, 强度: %.1f, StrengthZ: %.2f, 质量: %s, ATR评级: %s\n",
        zone.Type, zone.Strength, zone.StrengthZ, zone.Quality, zone.VolatilityGrade)
}
```

### 9.2 生成交易信号

```go
// 生成信号
signals := analyzer.GenerateSignals(sdData, currentPrice)

for _, signal := range signals {
    fmt.Printf("信号类型: %s\n", signal.Type)
    fmt.Printf("动作: %s\n", signal.Action)
    fmt.Printf("置信度: %.1f%%\n", signal.Confidence)
    fmt.Printf("原因: %s\n", signal.Reasoning)
}
```

### 9.3 自定义配置

```go
config := SDConfig{
    MinImpulsePercent: 0.005,  // 提高到0.5%
    MaxZoneAge: 300,           // 延长到300根K线
    MaxTouchCount: 5,          // 降低到5次
}

analyzer := &SupplyDemandAnalyzer{config: config}
sdData := analyzer.AnalyzeWithSymbol(klines, "BTCUSDT", "5m")
```

---

## 十、关键技术指标

1. **ATR归一化** - 跨币种统一波动率评估 (BTC vs 山寨币)
2. **Z-Score标准化** - 相对强度对比 (样本>=15)
3. **中位数成交量** - 抵御极端值影响
4. **模糊逻辑触及** - 容忍度缓冲区 (0.1×Zone_Width或0.05×ATR)
5. **真假突破判定** - 0.2×ATR缓冲距离 (防SFP)
6. **区域转换** - Breaker机制 (破损区转为支撑/阻力)

---

## 十一、总结

供需区分析系统是一个**生产级的Smart Money分析引擎**，特点包括：

✅ **严格的识别模式**: Drop-Base-Drop、Rally-Base-Rally等经典模式
✅ **ATR自适应**: 基于波动率的动态阈值，跨币种统一
✅ **Z-Score标准化**: 相对强度评估，避免绝对值误导
✅ **区域生命周期管理**: Fresh → Tested → Weakened → Broken
✅ **Breaker机制**: 突破后转换为反向支撑/阻力
✅ **数据清洗**: soft/hard双模式，平衡质量与完整性
✅ **7维上下文评分**: StrengthZ, WidthATR, VolRatio等全面评估

为AI提供机构交易者关注的关键价格区域，是Smart Money概念的核心实现。

---

**文档版本**: 1.0
**最后更新**: 2026-01-18
**维护者**: AI自动交易系统开发团队
