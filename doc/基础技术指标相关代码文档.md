# 基础技术指标相关代码完整文档

## 概述

本文档详细记录了AI自动交易系统中所有基础技术指标相关的逻辑代码,包括数据结构、计算方法、以及与AI分析的集成等模块。基础技术指标是市场分析的基石,为更高级的结构分析提供量化支持。

---

## 一、核心文件列表

### 1.1 主要技术指标文件

| 文件路径 | 行数 | 主要功能 |
|---------|------|---------|
| `market/data.go` | 3050+ | 所有基础技术指标计算实现 |
| `market/types.go` | 900+ | 所有数据结构定义 |
| `market/atr_manager.go` | 189 | ATR统一管理器,fallback机制 |
| `market/comprehensive_analysis.go` | 2572 | 多时间框架综合分析 |
| `market/data_quality_analyzer.go` | 400+ | 数据质量分析,异常值检测 |
| `market/strength_normalizer.go` | 1900+ | 强度归一化,Z-Score计算 |

### 1.2 支持工具文件

| 文件路径 | 功能 |
|---------|------|
| `market/utils.go` | 通用数学计算工具函数 |
| `market/data_cleaner.go` | 数据清洗和异常值过滤 |
| `market/json_contract_utils.go` | JSON序列化和数据初始化 |

---

## 二、数据结构定义

### 2.1 多时间框架数据结构

#### MultiTimeframeAnalysis - 多时间框架分析容器
```go
type MultiTimeframeAnalysis struct {
    Timeframes map[string]*TimeframeAnalysis  // 5m, 15m, 30m, 1h, 4h
    Summary    *AnalysisSummary               // 综合分析结果
}
```

#### TimeframeAnalysis - 单时间框架分析
```go
type TimeframeAnalysis struct {
    Timeframe       string
    DowTheory       *DowTheoryData     // 道氏理论
    ChannelAnalysis *ChannelData       // 通道分析
    VolumeProfile   *VolumeProfile     // 成交量分布
    SupplyDemand    *SupplyDemandData  // 供需区
    FairValueGaps   *FVGData           // 公平价值缺口
    Fibonacci       *FibonacciData     // 斐波纳契
    SupportResistance *SupportResistanceData  // 支撑阻力
    Weight          float64            // 时间框架权重
    Reliability     float64            // 可靠性评分
}
```

### 2.2 日内系列数据 (IntradayData)

**适用时间框架**: 5分钟

```go
type IntradayData struct {
    EMA20Values  []float64  // 20周期EMA序列 (最近10个)
    MACDValues   []float64  // MACD序列 (最近10个)
    RSI7Values   []float64  // 7周期RSI序列 (最近10个)
    RSI14Values  []float64  // 14周期RSI序列 (最近10个)
}
```

**用途**: 短期动量和趋势分析,快速反应市场变化

---

### 2.3 中期数据 (MediumTermData)

**适用时间框架**: 15分钟、30分钟、1小时

```go
type MediumTermData struct {
    // 移动平均线
    EMA20         float64    // 20周期EMA
    EMA50         float64    // 50周期EMA

    // 动量指标
    CurrentMACD   float64    // 当前MACD
    MACDValues    []float64  // MACD序列
    CurrentRSI7   float64    // 当前7周期RSI
    CurrentRSI14  float64    // 当前14周期RSI
    RSI14Values   []float64  // 14周期RSI序列

    // 波动率指标
    ATR14         float64    // 14周期ATR

    // OHLC数据
    OHLC          []OHLCData // 已收盘K线OHLC

    // 成交量分析
    AvgVolume     float64    // 平均成交量
    CurrentVolume float64    // 当前成交量
}
```

**用途**: 中期趋势确认,提供稳定的技术信号

---

### 2.4 长期数据 (LongerTermData)

**适用时间框架**: 4小时

```go
type LongerTermData struct {
    // 移动平均线
    EMA20       float64    // 20周期EMA
    EMA50       float64    // 50周期EMA

    // 波动率指标
    ATR3        float64    // 3周期ATR
    ATR14       float64    // 14周期ATR

    // 动量指标序列
    MACDValues  []float64  // MACD序列
    RSI14Values []float64  // 14周期RSI序列

    // 成交量分析
    AvgVolume     float64  // 平均成交量
    CurrentVolume float64  // 当前成交量
}
```

**用途**: 长期趋势识别和主要结构分析

---

### 2.5 SuperTrend结果结构

```go
type SuperTrendResult struct {
    Direction       string   // "bullish" 或 "bearish"
    CurrentLine     float64  // 当前趋势线价格
    UpperLine       float64  // 上轨价格
    LowerLine       float64  // 下轨价格
    IsValid         bool     // 数据有效性
    TrendDuration   int      // 趋势持续K线数
    FlipPrice       float64  // 最近翻转价格
    TrendStrength   float64  // 趋势强度评分 (0-100)
    Confidence      float64  // 置信度评分 (0-1)
    SignalQuality   string   // "high"|"medium"|"low"
    LastFlipTime    int64    // 最后翻转时间戳
}
```

---

## 三、核心技术指标算法

### 3.1 EMA (指数移动平均线)

**函数**: `calculateEMA(klines []Kline, period int) float64`

**位置**: market/data.go, 第324行

**计算步骤**:

1. **数据有效性检查**
   ```
   最少需要: period根K线
   建议数量: period × 3.5 (达到99%精度)
   ```

2. **初始EMA计算**
   ```go
   // 使用SMA作为第一个EMA值
   initialEMA = calculateSMA(klines[0:period], period)
   ```

3. **EMA迭代计算**
   ```go
   multiplier = 2.0 / (period + 1)

   for i := period; i < len(klines); i++ {
       price = klines[i].Close
       ema = (price - ema) * multiplier + ema
   }
   ```

**公式**:
```
EMA[t] = Price[t] × α + EMA[t-1] × (1-α)
其中 α = 2 / (period + 1)
```

**应用场景**:
- EMA20: 短期趋势识别
- EMA50: 中期趋势确认
- EMA20/50交叉: 趋势转换信号

---

### 3.2 SMA (简单移动平均线)

**函数**: `calculateSMA(klines []Kline, period int) float64`

**位置**: market/data.go, 第462行

**计算步骤**:

```go
// 1. 取最后period个K线的收盘价
sum := 0.0
for i := len(klines) - period; i < len(klines); i++ {
    sum += klines[i].Close
}

// 2. 计算平均值
sma := sum / float64(period)
```

**公式**:
```
SMA = (P1 + P2 + ... + Pn) / n
```

**应用场景**:
- EMA初始化
- SuperTrend ATR计算基础
- 简单趋势参考线

---

### 3.3 RSI (相对强弱指标)

**函数**: `calculateRSI(klines []Kline, period int) float64`

**位置**: market/data.go, 第372行

**计算步骤**:

1. **数据需求**
   ```
   最少: period + 1根K线
   建议: period × 3 (更准确的平滑)
   ```

2. **第一阶段: 初始平均值**
   ```go
   avgGain := 0.0
   avgLoss := 0.0

   for i := 1; i <= period; i++ {
       change := klines[i].Close - klines[i-1].Close
       if change >= 0 {
           avgGain += change
       } else {
           avgLoss += -change
       }
   }

   avgGain /= float64(period)
   avgLoss /= float64(period)
   ```

3. **第二阶段: Wilder平滑**
   ```go
   for i := period + 1; i < len(klines); i++ {
       change := klines[i].Close - klines[i-1].Close

       if change >= 0 {
           avgGain = (avgGain * (period-1) + change) / period
           avgLoss = (avgLoss * (period-1)) / period
       } else {
           avgGain = (avgGain * (period-1)) / period
           avgLoss = (avgLoss * (period-1) + (-change)) / period
       }
   }
   ```

4. **计算RSI**
   ```go
   if avgLoss == 0 {
       return 100.0
   }

   rs := avgGain / avgLoss
   rsi := 100.0 - (100.0 / (1.0 + rs))
   ```

**公式**:
```
RS = 平均涨幅 / 平均跌幅
RSI = 100 - (100 / (1 + RS))
```

**标准周期**:
- RSI7: 短期超买超卖
- RSI14: 标准周期

**应用场景**:
- RSI > 70: 超买警示
- RSI < 30: 超卖警示
- RSI背离: 趋势反转预警

---

### 3.4 MACD (移动平均收敛散离)

**函数**: `calculateMACD(klines []Kline) float64`

**位置**: market/data.go, 第352行

**计算步骤**:

1. **数据需求**
   ```
   最少: 26根K线
   建议: 91根 (26 × 3.5 达到99%精度)
   ```

2. **计算过程**
   ```go
   // 计算快线
   ema12 := calculateEMA(klines, 12)

   // 计算慢线
   ema26 := calculateEMA(klines, 26)

   // MACD = 快线 - 慢线
   macd := ema12 - ema26
   ```

**完整MACD系统**:
```
MACD线 = EMA12 - EMA26
信号线 = EMA(MACD, 9)
柱状图 = MACD线 - 信号线
```

**注**: 当前实现仅返回MACD值,不包括信号线和柱状图

**应用场景**:
- MACD > 0: 看涨动能
- MACD < 0: 看跌动能
- MACD交叉: 趋势转换信号

---

### 3.5 ATR (平均真实波幅)

#### 3.5.1 标准ATR计算

**函数**: `calculateATR(klines []Kline, period int) float64`

**位置**: market/data.go, 第422行

**计算步骤**:

1. **数据需求**
   ```
   最少: period + 1根K线
   建议: period × 2 (更稳定)
   ```

2. **计算True Range序列**
   ```go
   for i := 1; i < len(klines); i++ {
       // 方法1: 当日最高-最低
       range1 := klines[i].High - klines[i].Low

       // 方法2: 当日最高-前日收盘
       range2 := math.Abs(klines[i].High - klines[i-1].Close)

       // 方法3: 当日最低-前日收盘
       range3 := math.Abs(klines[i].Low - klines[i-1].Close)

       // 取最大值作为TR
       trueRange := max(range1, max(range2, range3))
       trueRanges = append(trueRanges, trueRange)
   }
   ```

3. **初始ATR: 使用SMA**
   ```go
   sum := 0.0
   for i := 0; i < period; i++ {
       sum += trueRanges[i]
   }
   atr := sum / float64(period)
   ```

4. **后续ATR: Wilder平滑**
   ```go
   for i := period; i < len(trueRanges); i++ {
       atr = ((atr * (period - 1)) + trueRanges[i]) / period
   }
   ```

**公式**:
```
TR = max(High-Low, |High-PrevClose|, |Low-PrevClose|)
ATR[1] = Average(TR[1:period])
ATR[t] = ((ATR[t-1] × (period-1)) + TR[t]) / period
```

---

#### 3.5.2 增强版ATR序列计算

**函数**: `calculateEnhancedATRSeries(klines []Kline, period int) []float64`

**位置**: market/data.go, 第2764行

**特点**:
- 返回完整ATR序列(用于SuperTrend)
- 使用Wilder's Smoothing (RMA)方法
- 与标准ATR保持一致性

---

#### 3.5.3 ATR管理器 (统一ATR管理)

**文件**: market/atr_manager.go

**核心数据结构**:
```go
type ATRManager struct {
    cache map[string]*ATREntry  // 按时间框架缓存
}

type ATREntry struct {
    Value      float64  // ATR值
    Mode       string   // "normal" | "fallback_pct" | "fallback_static"
    Confidence float64  // 0-1
    Timestamp  int64
}
```

**主要方法**:

1. **GetATR14**: 获取14期ATR,带fallback机制
   ```go
   func (m *ATRManager) GetATR14(klines, timeframe, currentPrice) *ATREntry

   尝试顺序:
   1. 标准ATR计算 (需>=15根K线)
   2. Fallback模式1: 使用3期ATR
   3. Fallback模式2: 基于价格百分比估算 (1.5%)
   4. Fallback模式3: 固定值估算
   ```

2. **GetDistanceATR**: 计算距离级别的ATR倍数
   ```go
   func (m *ATRManager) GetDistanceATR(level, currentPrice, timeframe, klines) (float64, string)

   返回: (ATR倍数, fallback模式标识)
   ```

**Fallback机制**:
```
优先级1: 正常计算 (confidence=1.0)
    ↓ 数据不足
优先级2: ATR3计算 (confidence=0.7)
    ↓ 仍不足
优先级3: 百分比估算 (confidence=0.4)
    ↓ 最后
优先级4: 静态估算 (confidence=0.2)
```

**应用价值**:
- 解决各模块ATR计算不一致问题 (P0-3修复)
- 确保ATR在数据不足时也能提供合理值
- 所有需要ATR的模块统一调用

---

### 3.6 SuperTrend (超级趋势线)

**函数**: `calculateSupertrendEnhanced(klines []Kline, timeframe string, customParams ...float64) SuperTrendResult`

**位置**: market/data.go, 第2612行

#### 3.6.1 动态参数配置

**函数**: `getDynamicSupertrendParams(timeframe string) (int, float64)`

**位置**: market/data.go, 第2736行

**参数表**:
| 时间框架 | ATR周期 | Factor | 特点 |
|---------|--------|--------|------|
| 1m-5m | 10 | 3.0 | 低时间框架-敏感 |
| 15m-30m | 14 | 3.5 | 中时间框架-平衡 |
| 1h-4h | 20 | 4.0 | 高时间框架-稳定 |
| 12h-1d | 14 | 5.0 | 超高时间框架-最稳定 |

**说明**: P0-04修复确保参数真正生效,避免"伪配置"

---

#### 3.6.2 核心计算步骤

```go
1. 获取动态参数
   atrPeriod, factor := getDynamicSupertrendParams(timeframe)

2. 计算增强版ATR序列
   atrs := calculateEnhancedATRSeries(klines, atrPeriod)

3. 计算上下轨线
   for i := range klines {
       // 中间价
       hl2 := (klines[i].High + klines[i].Low) / 2

       // 基础带
       basicUpperBand := hl2 + (factor * atrs[i])
       basicLowerBand := hl2 - (factor * atrs[i])

       // 带平滑处理 (减少假信号)
       if i > 0 {
           if basicUpperBand < finalUpperBand[i-1] || klines[i-1].Close > finalUpperBand[i-1] {
               finalUpperBand[i] = basicUpperBand
           } else {
               finalUpperBand[i] = finalUpperBand[i-1]
           }

           if basicLowerBand > finalLowerBand[i-1] || klines[i-1].Close < finalLowerBand[i-1] {
               finalLowerBand[i] = basicLowerBand
           } else {
               finalLowerBand[i] = finalLowerBand[i-1]
           }
       }
   }

4. 趋势方向确定
   if close > finalUpperBand[i-1] {
       direction = "bullish"
       currentLine = finalLowerBand[i]
   } else if close < finalLowerBand[i-1] {
       direction = "bearish"
       currentLine = finalUpperBand[i]
   }

5. 计算趋势强度、置信度、信号质量
   trendStrength = calculateTrendStrength(...)
   confidence = calculateConfidence(...)
   signalQuality = determineSignalQuality(...)
```

---

#### 3.6.3 趋势强度评分

**函数**: `calculateTrendStrength(klines, directions, supertrendLines, duration, lastIdx) float64`

**位置**: market/data.go, 第2888行

**5个因子加权平均**:

```go
总分 = 价格距离强度(30%) + 趋势一致性(25%) + 成交量支撑(20%)
     + 持续时间(15%) + 价格动量(10%)

1. 价格距离强度 (30分):
   distance = |close - trendLine|
   distanceScore = (distance / close) * 100 * 30

2. 趋势一致性强度 (25分):
   consistency = 连续同向K线数 / 总K线数
   consistencyScore = consistency * 25

3. 成交量支撑强度 (20分):
   volumeRatio = avgRecentVolume / avgHistoricalVolume
   volumeScore = min(volumeRatio, 2.0) * 10

4. 持续时间强度 (15分):
   durationScore = min(duration / 10, 1.0) * 15

5. 价格动量强度 (10分):
   momentum = |最近5根close变化| / 5
   momentumScore = (momentum / close) * 1000
```

**范围**: 0-100

---

#### 3.6.4 置信度评分

**函数**: `calculateConfidence(klines, atrs, duration, flipPoints, lastIdx) float64`

**位置**: market/data.go, 第2965行

**5个因子加权平均**:

```go
置信度 = 数据充分性(25%) + 趋势稳定性(20%) + 持续时间置信度(20%)
       + ATR相对强度(15%) + 价格行为一致性(20%)

1. 数据充分性 (0.25):
   sufficiency = min(totalBars / 100, 1.0) * 0.25

2. 趋势稳定性 (0.20):
   stability = (1.0 - flipCount/duration) * 0.20

3. 持续时间置信度 (0.20):
   durationConf = min(duration / 20, 1.0) * 0.20

4. ATR相对强度 (0.15):
   atrStrength = min(currentATR / avgATR, 2.0) / 2.0 * 0.15

5. 价格行为一致性 (0.20):
   behaviorConsistency = 连续同向占比 * 0.20
```

**范围**: 0.0-1.0

---

#### 3.6.5 信号质量判断

**函数**: `determineSignalQuality(trendStrength, confidence, duration, totalBars int) string`

**位置**: market/data.go, 第3041行

**评分公式**:
```go
综合评分 = TrendStrength×0.4 + Confidence×100×0.3
         + (Duration/10×10)×0.2 + (TotalBars/100×100)×0.1
```

**质量分级**:
```go
if score >= 70 && trendStrength >= 60 && confidence >= 0.7:
    return "high"
else if score >= 50 && trendStrength >= 40 && confidence >= 0.5:
    return "medium"
else:
    return "low"
```

---

### 3.7 VWAP (成交量加权平均价)

**函数**: `calculateVWAP(klines []Kline) float64`

**位置**: market/data.go, 第475行

**计算步骤**:

```go
volumeWeightedSum := 0.0
totalVolume := 0.0

for _, kline := range klines {
    // 1. 计算典型价格
    typicalPrice := (kline.High + kline.Low + kline.Close) / 3.0

    // 2. 累积成交量加权和
    volumeWeightedSum += typicalPrice * kline.Volume
    totalVolume += kline.Volume
}

// 3. 计算VWAP
vwap := volumeWeightedSum / totalVolume
```

**公式**:
```
TP = (High + Low + Close) / 3
VWAP = Σ(TP × Volume) / Σ(Volume)
```

**应用场景**:
- 盘中平均成本参考
- 大单进出场判断
- 价格效率评估

---

### 3.8 派生指标

#### 3.8.1 EMA斜率

**函数**: `calculateEMASlope(klines []Kline, period int, lookback int) float64`

**位置**: market/data.go, 第498行

**计算过程**:
```go
// 1. 计算当前EMA
currentEMA := calculateEMA(klines, period)

// 2. 计算lookback根前的EMA
prevKlines := klines[:len(klines)-lookback]
prevEMA := calculateEMA(prevKlines, period)

// 3. 计算变化率
slope := ((currentEMA - prevEMA) / prevEMA) * 100
```

**用途**: 衡量趋势动力强度,判断趋势加速/减速

---

## 四、多时间框架数据计算

### 4.1 日内系列数据计算

**函数**: `calculateIntradaySeries(klines []Kline) *IntradayData`

**适用**: 5分钟时间框架

**计算内容**:
```go
intradayData := &IntradayData{
    EMA20Values:  collectLast10(calculateEMA(klines, 20)),
    MACDValues:   collectLast10(calculateMACD(klines)),
    RSI7Values:   collectLast10(calculateRSI(klines, 7)),
    RSI14Values:  collectLast10(calculateRSI(klines, 14)),
}
```

**特点**: 保留最近10个数据点,用于短期趋势和动量分析

---

### 4.2 中期数据计算

**函数**: `calculateMediumTermData(klines []Kline, timeframe string) *MediumTermData`

**位置**: market/data.go, 第2514行

**适用**: 15分钟、30分钟、1小时

**计算内容**:
```go
mediumData := &MediumTermData{
    // 移动平均线
    EMA20: calculateEMA(klines, 20),
    EMA50: calculateEMA(klines, 50),

    // 动量指标
    CurrentMACD:  calculateMACD(klines),
    MACDValues:   calculateMACDSeries(klines, lastN),
    CurrentRSI7:  calculateRSI(klines, 7),
    CurrentRSI14: calculateRSI(klines, 14),
    RSI14Values:  calculateRSISeries(klines, 14, lastN),

    // 波动率
    ATR14: calculateATR(klines, 14),

    // OHLC (已收盘K线)
    OHLC: extractOHLC(klines),

    // 成交量
    AvgVolume:     calculateAverageVolume(klines, 20),
    CurrentVolume: klines[len(klines)-1].Volume,
}
```

---

### 4.3 长期数据计算

**函数**: `calculateLongerTermData(klines []Kline) *LongerTermData`

**位置**: market/data.go, 第566行

**适用**: 4小时时间框架

**计算内容**:
```go
longerData := &LongerTermData{
    // 移动平均线
    EMA20: calculateEMA(klines, 20),
    EMA50: calculateEMA(klines, 50),

    // 多周期ATR
    ATR3:  calculateATR(klines, 3),
    ATR14: calculateATR(klines, 14),

    // 动量指标序列
    MACDValues:  calculateMACDSeries(klines, lastN),
    RSI14Values: calculateRSISeries(klines, 14, lastN),

    // 成交量
    AvgVolume:     calculateAverageVolume(klines, 20),
    CurrentVolume: klines[len(klines)-1].Volume,
}
```

---

## 五、技术指标在系统中的应用

### 5.1 数据质量分析集成

**文件**: market/data_quality_analyzer.go

**使用ATR进行异常值检测**:
```go
type OutlierDistributionStats struct {
    WidthATROutliers *OutlierTypeStats  // 基于ATR倍数的异常值
}
```

**质量评分因素**:
- ATR稳定性
- 数据一致性
- 异常值集中度

---

### 5.2 强度归一化中的指标集成

**文件**: market/strength_normalizer.go

```go
type StrengthNormalizer struct {
    // 基于ATR倍数的宽度统计
    HistoryWidths []float64  // 滑动窗口：最近200个宽度数据(ATR倍数)
}
```

**Z-Score计算用于**:
- 区域宽度评估
- 异常信号检测
- 交易信号可靠性评分

---

### 5.3 触发器检测中的指标应用

**文件**: market/trigger_detector.go

**利用技术指标的触发条件**:
- SuperTrend方向反转作为确认信号
- ATR距离用于止损止盈设置
- EMA/RSI用于信号过滤

---

### 5.4 AI提示词中的指标引用

**提示词模板位置**: /prompts/adaptive.txt, /prompts/taro_long_prompts.txt

**关键指标在提示词中的使用**:
- EMA作为趋势方向确认
- RSI用于超买超卖判断
- MACD用于动量确认
- ATR用于风险管理(止损设置)
- SuperTrend用于入场/出场确认

---

## 六、数据流向和时间同步

### 6.1 K线数据获取

```go
// 获取多个时间框架的K线
klines5m, _ := WSMonitorCli.GetCurrentKlines(symbol, "5m")
klines15m, _ := WSMonitorCli.GetCurrentKlines(symbol, "15m")
klines30m, _ := WSMonitorCli.GetCurrentKlines(symbol, "30m")
klines1h, _ := WSMonitorCli.GetCurrentKlines(symbol, "1h")
klines4h, _ := WSMonitorCli.GetCurrentKlines(symbol, "4h")

// 🔥 P0-01修复：时间锚点裁剪
// 确保所有时间框架基于同一时间基准
klines5m = filterKlinesByAnchorTime(klines5m, anchorCloseTimeMs)
klines15m = filterKlinesByAnchorTime(klines15m, anchorCloseTimeMs)
// ... 其他时间框架
```

---

### 6.2 指标计算流程

```
K线数据 (带时间锚点)
    ↓
基础指标计算 (EMA, MACD, RSI, ATR)
    ↓
派生指标计算 (SuperTrend, TrendStrength, Confidence)
    ↓
多时间框架聚合 (综合各时间框架结果)
    ↓
AI分析输入 (作为提示词的背景信息)
    ↓
交易决策输出
```

---

## 七、关键优化和修复

### 7.1 P0-01修复：时间一致性
- 使用锚点时间裁剪K线，确保所有指标基于同一时间基准
- 避免指标漂移导致的错误信号

### 7.2 P0-02修复：多时间框架HTF结构
- 添加最新已收盘4h K线，避免结构判断滞后

### 7.3 P0-03修复：K线数据缓存
- 在Get()阶段缓存K线数据，供后续使用
- 避免FormatAsCompactData二次获取导致的数据漂移

### 7.4 P0-3修复：统一ATR管理
- 建立ATRManager统一管理所有ATR计算
- 包含fallback机制，确保ATR计算一致性
- 解决各模块ATR计算不一致的问题

### 7.5 P0-04修复：SuperTrend参数配置
- 根据时间框架动态调整参数
- 避免"伪配置"，确保参数真正生效

---

## 八、关键函数索引

| 函数 | 文件 | 行号 | 功能 |
|------|------|------|------|
| `calculateEMA` | data.go | 324 | EMA计算 |
| `calculateSMA` | data.go | 462 | SMA计算 |
| `calculateRSI` | data.go | 372 | RSI计算 |
| `calculateMACD` | data.go | 352 | MACD计算 |
| `calculateATR` | data.go | 422 | ATR计算 |
| `calculateEnhancedATRSeries` | data.go | 2764 | 增强版ATR序列 |
| `calculateSupertrendEnhanced` | data.go | 2612 | SuperTrend增强版 |
| `calculateVWAP` | data.go | 475 | VWAP计算 |
| `calculateEMASlope` | data.go | 498 | EMA斜率 |
| `calculateTrendStrength` | data.go | 2888 | 趋势强度评分 |
| `calculateConfidence` | data.go | 2965 | 置信度评分 |
| `determineSignalQuality` | data.go | 3041 | 信号质量判断 |
| `GetATR14` | atr_manager.go | 32 | 统一ATR获取 |
| `calculateIntradaySeries` | data.go | 521 | 日内系列数据 |
| `calculateLongerTermData` | data.go | 566 | 长期数据 |
| `calculateMediumTermData` | data.go | 2514 | 中期数据 |

---

## 九、使用示例

### 9.1 计算单个指标

```go
// EMA计算
ema20 := calculateEMA(klines, 20)
ema50 := calculateEMA(klines, 50)

// RSI计算
rsi7 := calculateRSI(klines, 7)
rsi14 := calculateRSI(klines, 14)

// MACD计算
macd := calculateMACD(klines)

// ATR计算
atr14 := calculateATR(klines, 14)
```

### 9.2 使用ATR管理器

```go
// 创建ATR管理器
atrManager := NewATRManager()

// 获取ATR (带fallback)
atrEntry := atrManager.GetATR14(klines, "5m", currentPrice)

fmt.Printf("ATR: %.2f, Mode: %s, Confidence: %.2f\n",
    atrEntry.Value, atrEntry.Mode, atrEntry.Confidence)

// 计算距离ATR
distATR, mode := atrManager.GetDistanceATR(level, currentPrice, "5m", klines)
fmt.Printf("Distance: %.2f ATR, Mode: %s\n", distATR, mode)
```

### 9.3 计算SuperTrend

```go
// 使用动态参数
result := calculateSupertrendEnhanced(klines, "5m")

fmt.Printf("Direction: %s\n", result.Direction)
fmt.Printf("Current Line: %.2f\n", result.CurrentLine)
fmt.Printf("Trend Strength: %.1f\n", result.TrendStrength)
fmt.Printf("Confidence: %.2f\n", result.Confidence)
fmt.Printf("Signal Quality: %s\n", result.SignalQuality)

// 使用自定义参数
customResult := calculateSupertrendEnhanced(klines, "5m", 10, 3.0)
```

### 9.4 计算多时间框架数据

```go
// 日内数据 (5m)
intradayData := calculateIntradaySeries(klines5m)
fmt.Printf("Latest EMA20: %.2f\n", intradayData.EMA20Values[len(intradayData.EMA20Values)-1])

// 中期数据 (1h)
mediumData := calculateMediumTermData(klines1h, "1h")
fmt.Printf("EMA20: %.2f, EMA50: %.2f\n", mediumData.EMA20, mediumData.EMA50)
fmt.Printf("RSI14: %.2f, MACD: %.4f\n", mediumData.CurrentRSI14, mediumData.CurrentMACD)

// 长期数据 (4h)
longerData := calculateLongerTermData(klines4h)
fmt.Printf("ATR3: %.2f, ATR14: %.2f\n", longerData.ATR3, longerData.ATR14)
```

---

## 十、总结

本文档涵盖了nofx项目中所有主要基础技术指标的实现细节、数据结构、计算方法以及在AI分析中的集成方式。关键特点包括:

✅ **完整的指标体系**: EMA、SMA、RSI、MACD、ATR、SuperTrend、VWAP等
✅ **多时间框架支持**: 5m、15m、30m、1h、4h全覆盖
✅ **统一ATR管理**: 解决各模块计算不一致问题
✅ **动态参数配置**: 根据时间框架自适应调整
✅ **时间锚点同步**: 确保多时间框架数据一致性
✅ **质量评估体系**: 趋势强度、置信度、信号质量三维评估
✅ **AI友好设计**: 所有指标都可序列化为JSON供AI使用

---

**文档版本**: 1.0
**最后更新**: 2026-01-18
**维护者**: AI自动交易系统开发团队
